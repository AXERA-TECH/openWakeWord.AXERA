# Weights

将 openWakeWord 原始 ONNX 模型放到 `weights/openwakeword/`：

```text
alexa_v0.1.onnx
embedding_model.onnx
hey_jarvis_v0.1.onnx
hey_mycroft_v0.1.onnx
hey_rhasspy_v0.1.onnx
melspectrogram.onnx
timer_v0.1.onnx
weather_v0.1.onnx
```

`silero_vad.onnx` 可选，仅导出静态 ONNX，不参与默认量化。
